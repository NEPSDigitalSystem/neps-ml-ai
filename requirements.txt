# NEPS ML/AI Requirements
fastapi
uvicorn
prometheus-fastapi-instrumentator

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Add your ML dependencies here (e.g., pandas, scikit-learn, tensorflow, etc.)
# For now, the mock data doesn't require any special dependencies!
